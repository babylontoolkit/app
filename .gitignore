logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
.vscode/launch.json
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

/.history
/.cache
/build
functions/build/
.env.local
.env
# ⚠️ EVERY .env variant EXCEPT the checked-in example. `.env.local` + `.env` alone left
# `.env.production` tracked — the most dangerous file in the family, and the one that does not end in
# `local` (the same blind spot `isSecretPath` had in git/sync-logic.ts). Deployed values come from
# SSM -> container env (DEPLOY.md); no .env file belongs in the repo but the example.
.env.*
!.env.example
.dev.vars
*.vars
.wrangler
_worker.bundle

Modelfile
modelfiles

# docs ignore
site

# commit file ignore
app/commit.json
changelogUI.md
docs/instructions/Roadmap.md
.cursorrules
.qodo

# Platform data store (prompt versions, skills, generations) — local dev backend for the
# Supabase/S3 layer that lands in Stage 2. Never commit.
.data/
