{
  "name": "@babylonjs-toolkit/bridge",
  "version": "0.0.1",
  "description": "Local companion that lets the browser-based app builder drive your Unity Editor over MCP.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "bridge": "./src/cli.js",
    "babylonjs-toolkit-bridge": "./src/cli.js"
  },
  "files": [
    "src",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test src/*.test.js",
    "deploy-all": "npm publish --access public"
  }
}
